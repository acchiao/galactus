# Version control / CI
.git
.gitattributes
.gitignore
.github
.gitlab-ci.yml
.travis.yml

# Python
.venv
venv
__pycache__
*.py[cod]
.mypy_cache
.ruff_cache
.pytest_cache
.cache

# Node / frontend deps (reinstalled in the image)
node_modules

# Frontend build outputs (regenerated by the client build stage)
galactus/static/webpack_bundles/
webpack-stats.json

# Django collectstatic output (regenerated in the image)
staticfiles

# Local env files / secrets
.envs/

# Editor / OS / tooling config not needed in the image
.idea
.vscode
.DS_Store
.editorconfig
.pre-commit-config.yaml
.prettierrc
.prettierignore
.readthedocs.yml
.devcontainer

# Kubernetes/Helm (not needed at runtime)
charts
